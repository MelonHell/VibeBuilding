"""Measurements of this building's references, run by hand and read by a person.

The measuring itself is in `blockwright.measure`. What lives here is the choice of
window, band and threshold for *this* building -- which is a fact about this
building and not about measurement, so it belongs beside the build rather than in
the library.
"""
