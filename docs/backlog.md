# Что осталось, и сколько раз это уже просили

Пять зданий написали разбор, в каждом есть раздел «что стоит доработать», и
несколько пунктов в них повторяются дословно. Повторяются они потому, что
записать их было некуда: разбор лежит в папке здания, следующее здание заводят
из шаблона, и человек, который туда попадает, читает README, а не чужие
ЗАМЕТКИ.

Это тот файл. Одна строка на пункт, со зданиями, которые его подняли, и
счётчиком. **Пункт, поднятый трижды, — это не пожелание, а свойство метода**, и
чинить его надо в библиотеке.

Порядок — по счётчику, потом по цене.

---

## Открытые

### Пять зданий на своих копиях нарезки кровли — 5 зданий
`002`, `003`, `004`, `005`, `006`

**Единственный крупный пункт, который остался.** `blockwright/roof.py` — 346
строк ровно этого аппарата (`terraces`, `deck`, `plant`, `profile`, `plateau`),
импортирует его одно здание из шести. Остальные держат свои: `bands`+`cells`
(003), `roof_step`+`_field`+`_deck`+`_lumps` (004), `roof_grid` (002),
`probes.roof` (006). Около 500 строк, четыре разных ответа на один вопрос.

`005` уже переведено: его `_steps` теперь `measure.flatten` плюс заливка дыр,
90 строк стали 40.

**Почему не сделано разом:** миграция меняет измеренную геометрию, а проверить
её можно только прогнав `derive` — и `002`, `003`, `004` сейчас на нём
останавливаются об отказ по датуму (ниже). Мигрировать то, что нельзя
перемерить, — это менять числа вслепую. Сначала переклип, потом миграция.

### Пять зданий из шести объявили `SINGULAR = True` — 5 зданий
`002`, `003`, `004`, `004_2`, `006`

Аварийный выход из `evenness` стоит почти везде, то есть механизм фактически
выключен по всему проекту. Формально каждый раз правда — крылья разной длины,
корпуса непохожи, — но строка, которую все обходят одинаково, перестала быть
вопросом. Стоит посмотреть, не задаёт ли `TWINS` слишком узкий вопрос: он
требует **зеркальную пару**, а повторяются в этих зданиях этажи, ряды вилл и
секции фасада, для которых зеркала нет.

Смежное: `facades` (новая строка) задаёт обратный вопрос и своим выходом
(`UNIFORM`) пока не пользуется никто — за этим стоит следить, чтобы он не
повторил судьбу `SINGULAR`.

### `skyline_of` не говорит, что часть перекрыта соседом — 1 раз
`005`

`low 5.2, median 27.6, high 29.8` на плоской двухэтажной крыше — разброс в 24 м,
видный невооружённым глазом и не выведенный ни в одну строку.

### Профиль эталона и профиль постройки рядом, в одних единицах — 1 раз
`005`

Обе таблицы писались в `out/scratch` руками, и обе решали вопрос за один взгляд
после часа перебора. Место им в `gate.py` под флагом. Это же превратило бы «не
циклиться» из совета в команду.

---

## Закрытые, и чем

| пункт | кто просил | чем закрыт |
|---|---|---|
| ровность: пила по кромке, разъехавшаяся пара, крыша-лестница | — | `Mask.straighten` + `Site.STRAIGHT`; `checks.jaggedness` / `twins` / `level_runs` строками гейта; `Frame.flipped` по u и по v |
| дубль констант гейта | — | `grading.DEFAULTS`; бюджет по умолчанию помечается «(default, not measured here)» |
| единицы станций сечения | `004`, `005` | `gate.station_of` / `gate.build_bin` |
| `survey.storeys_of` берёт полосы не с той оси | `005` | длинные грани всего плана; плюс текстура бьёт слабую геометрию (`STOREY_SURE`, `TEXTURE_SIDES`) |
| датум: рама подогналась к коробке клипа | `005`, `006` | `survey.fits_clip_box` пишет, `Grading.clip_box` читает — строка перестала быть мёртвой |
| `flatmap.parcel` не работает на реальных кропах | `003`, `004`, `006` | `flatmap.surrounds` и `flatmap.surfaces`; `002` и `004` мигрированы с рукописных циклов по пикселям |
| пальма и рассев — примитивы, а не деталь здания | `003`, `004`, `005` | `build.palm`, `build.scatter`; `002` мигрировано с отбора по `n % 23` |
| `largest_rect` — примитив | `006` | `Mask.largest_rect` (на сетке мира); версия 006 в (u,v) осталась и это правильно |
| ложный этаж 4.5 м — сетка тайлера | `004`, `005`, `006` | `measure.looks_like_tiling`, `TILE_SEAM` |
| чек-лист первой конвертации | `004`, `006` | `blockwright-capture`, три пункта: heading, датум, тайлинг |
| мода высот вместо медианы | `004`, `005` | `measure.mode` |
| исключение «дыра в захвате» | `004`, `005` | `gate.capture_hole`, `gate.one_station_off` |
| одна регистрация, а не две | `002`, `005` | `grading.Grading.registration`, `REGISTRATION = "measured"` по умолчанию |
| `_template/gate.py` жёстко пишет `floor=6.0` | `004` | `grading` читает `derive.MESH_FLOOR` |
| шаблон учит вешать балкон полкой | `004` | `build.recess` и абзац в `blockwright-draw` |
| ревью делает та же сессия | `004`, `006` | `blockwright-photo-reviewer` объявлен шагом стадии в скилле и в команде |
| `DESCRIPTION` остаётся плейсхолдером | `003`, `004`, `005` | отказ в `reviewing.check_written` |
| `Item.blocks` не говорит, что реально стоит | `005` | аудит печатает объявлено-против-найдено и называет чужие блоки |
| рассев по модулю — решётка, а не монетка (диагональ повторяется в 82 %) | `003`, `006` | `Mask.speckle`; пять мест мигрированы; `tools/style_selftest.py` следит |
| горизонтальные поверхности — один печатный прямоугольник одного блока | `002`, `003`, `005` (замером, не заявкой) | `build.dither` + `blocks.pairs` / `why_pair`; `blockwright/style.py` печатает `texture` на каждом прогоне |
| таблица цветов не знает половины серых и тёмного остекления | — | 14 блоков в `blocks.COLORS`; `_slab`/`_stairs`/`_wall` выводятся ещё и от `_block` и от множественного числа |
| ритм фасада — одно число на здание, и крыло с другим фасадом усредняется | `004_2` | `measure.rhythm_by_span` + `measure.apart`; несогласные орто теперь называются поимённо |
| ничто не спрашивает, согласен ли эталон с тем, что постройка сделала одинаковым | `004_2` | строка гейта `facades` (`FACADES` / `FACADES_DIFFER` / `UNIFORM`), `checks.facade_mix` по плоскости стены вместе с воздухом |
| ярусы сливаются по габаритам, поэтому коробка+рама выходит одной коробкой | `004_2` | `roof.woven` / `roof.one_surface` — по касанию масок, а не по габаритам |
| на крышу можно поставить только замкнутую коробку | `004_2` | `build.rack` (стойки, настил, аппараты) и `Site.grid` для сетки стоек в раме |
| выпрямление выключено умолчанием, и здание начинается с пилы | — | `Site.STRAIGHT = 1.0` по умолчанию; `Mask.open` / `round` — пары к `close`, которой не было; `Mask.mirrored` / `symmetrise` принуждают симметрию, а не сообщают о ней; `tools/evenness_selftest.py` |
| `straighten` возвращал форму на 6 % меньше, а `jaggedness` это же и докладывал как шум чертежа | — | `Mask._offset_ring`; контур читается по центрам клеток, кольцо выталкивается обратно на полклетки перед растеризацией. Снимать вместе с ним ручные компенсации: у `004_3` стоял `PAD = 1.5` ровно за этим |
| словарь некубических блоков выучил рендерер, а постройки нет: ступени 0.000 против 0.005, плиты 0.000–0.019 против 0.080, ограждение полным кубом | — | `build.balustrade` (с отказом на куб), `build.cornice` (с `opposite` и с `Facade` по вынесенной маске), `build.kerb`, `build.pave`; `tools/relief_selftest.py` читает готовый холст через `blocks.boxes` |
| `style.texture` считала горизонтальной поверхностью перекрытие каждого этажа, а эталон мерян по кровлям — колонки отвечали на разные вопросы | — | `style._sky`: поверхность это верхний блок колонны. Корпус перемерян: горизонталь 0.009–0.150, фасад 0.08–0.27. Улучшение на 004_3 читается 0.142 по старому счёту и 0.386 по новому |
| перила, кашпо, ступени, фонари — открытые находки на каждом здании, и класть их нечем | `001`, `008` | `build.lamp`, `build.planter`, `build.steps` (марш с подступенком, иначе ступени висят); позиция у всех выводится из геометрии, фотография тут ни при чём |
| `weathered_copper` объявлен дважды с **разными** значениями, `deepslate` и `orange_concrete` задвоены; модуль с политикой «неизвестное — маджента» обязан быть однозначным | — | `tools/palette_selftest.py` читает исходник через `ast` и ловит повтор ключа, которого сам словарь после импорта уже не видит. Оставлено значение, которое действовало, чтобы дедупликация ничего не перекрасила молча |
| нет бледного ствола: у королевской пальмы стебель серо-белый, в таблице только `jungle_log` и `oak_log` | `008` | `birch_log`, `stripped_birch_log`, `stripped_oak_log` |
| «фотография доказывает, что часть есть, и позиции не даёт» — часть просто не строят, и в рендере это неотличимо от «её нет» | `001`, `008` | `Item(..., placed=...)`: позиция как **счёт от измеренного якоря**, не метры; строка гейта `placement` (`[----]` с причиной, прогон `ungraded`; при пустом списке проходит и говорит об этом, а не молчит) |

---

## Как этим пользоваться

Заводя здание — прочитать «Открытые». Это не документация, это список граблей,
на которые уже наступили, с указанием, сколько раз.

Закончив здание — дописать разбор в папку здания **и** строку сюда. Если строка
уже есть, поднять счётчик и приписать своё здание. Ничего больше делать не надо:
счётчик и есть механизм.

Закрывая пункт — перенести его в таблицу с тем, чем закрыт. Пункт, закрытый без
строки в таблице, вернётся: пять зданий подряд писали «`flatmap.parcel` не
работает», а `surrounds` к тому моменту уже был написан.
